version: '3'

services:
  app:
    build:
      context: apps/repos-api
    ports:
      - '3000:3000'
    env_file:
      - apps/repos-api/.env
    depends_on:
      - redis
    environment:
      REDIS_URL: redis://redis:6379

  redis:
    image: redis:latest
    ports:
      - '6379:6379'
